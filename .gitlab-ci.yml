image: node:latest

cache:
  paths:
    - node_modules/

before_script:
  - npm install

pages:
  stage: deploy
  script:
    - npm run build
    - cp -r public_html/* public/
  artifacts:
    paths:
      - public
  only:
    - main
